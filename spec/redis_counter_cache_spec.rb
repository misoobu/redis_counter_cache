require 'spec_helper'

describe RedisCounterCache do
  it 'has a version number' do
    expect(RedisCounterCache::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
